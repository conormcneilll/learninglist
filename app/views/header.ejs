<div class="header">
    <div class="logo">
        <img src="logo.png" alt="Logo">
        <span>Learning Lists</span>
    </div>
    <div class="buttons">
        <% if (!loggedIn) { %>
            <a href="/login">Login</a>
            <a href="/signup">Sign Up</a>
        <% } else { %>
            <a href="/logout">Logout</a>
        <% } %>
    </div>
</div>
